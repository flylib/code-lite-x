// Auto-generated by xtask codegen. DO NOT EDIT.
#![allow(clippy::all, unused_imports)]

use code_lite_rpc::protocol::{error_codes, JsonRpcRequest, JsonRpcResponse};
use crate::CodeLiteContext;
use super::types::*;

/// Dispatches an incoming JSON-RPC request to the appropriate handler.
pub fn dispatch_rpc(ctx: *mut CodeLiteContext, req: &JsonRpcRequest) -> JsonRpcResponse {
    match req.method.as_str() {
        "file.open" => {
            let params: FileOpenParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::file::handle_open(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "file.edit" => {
            let params: FileEditParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::file::handle_edit(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "file.undo" => {
            let params: FileUndoParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::file::handle_undo(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "file.redo" => {
            let params: FileRedoParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::file::handle_redo(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "lsp.completion" => {
            let params: LspCompletionParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::lsp::handle_completion(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "lsp.diagnostics" => {
            let params: LspDiagnosticsParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::lsp::handle_diagnostics(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "agent.send_prompt" => {
            let params: AgentSendPromptParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::agent::handle_send_prompt(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        "git.status" => {
            let params: GitStatusParams = match req.params.as_ref() {
                Some(val) => match serde_json::from_value(val.clone()) {
                    Ok(p) => p,
                    Err(e) => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, e.to_string()),
                },
                None => return JsonRpcResponse::err(req.id, error_codes::INVALID_PARAMS, "Missing required params"),
            };
            match crate::handlers::git::handle_status(ctx, params) {
                Ok(res) => JsonRpcResponse::success(req.id, serde_json::to_value(res).unwrap()),
                Err(err) => JsonRpcResponse::err(req.id, error_codes::SERVER_ERROR, err),
            }
        }
        _ => JsonRpcResponse::err(
            req.id,
            error_codes::METHOD_NOT_FOUND,
            format!("Method not found: {}", req.method),
        ),
    }
}
